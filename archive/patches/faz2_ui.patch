--- a/templates/index.html
+++ b/templates/index.html
@@ -212,7 +212,7 @@
         </div>
         <button type="button" class="adv-toggle" onclick="toggleAdv()">Gelişmiş olasılık (isteğe bağlı) ▾</button>
         <div class="adv-panel" id="advPanel">
-          <div class="adv-hint">Her maç için 1/0/2 olasılık (0–1 veya %). Boş = uniform. Bayes: prior + evidence → posterior → exact/MC.</div>
+          <div class="adv-hint">Her maç için 1/0/2 olasılık (0–1 veya %). Boş = uniform. Bayes açıkken: prior + evidence → posterior → exact/MC (14-garanti değişmez).</div>
           <div class="adv-grid">
             {% for i in range(1, match_count + 1) %}
             <div class="adv-row">
@@ -225,9 +225,20 @@
           </div>
           <div class="bayes-opts">
             <label class="bayes-check"><input type="checkbox" name="use_bayes" value="1" id="useBayes" {% if form_data and form_data.get('use_bayes') == '1' %}checked{% endif %}> Bayes güncelle (Dirichlet)</label>
-            <label>Prior α <input type="number" name="prior_strength" min="0.1" max="50" step="0.5" value="{{ form_data.get('prior_strength', 1) if form_data else 1 }}"></label>
-            <label>Evidence n <input type="number" name="evidence_strength" min="0" max="200" step="1" value="{{ form_data.get('evidence_strength', 10) if form_data else 10 }}"></label>
+            <label>Prior α <input type="number" name="prior_strength" id="priorStrength" min="0.1" max="50" step="0.5" value="{{ form_data.get('prior_strength', 1) if form_data else 1 }}"></label>
+            <label>Evidence n <input type="number" name="evidence_strength" id="evidenceStrength" min="0" max="200" step="1" value="{{ form_data.get('evidence_strength', 10) if form_data else 10 }}"></label>
+            <label>Preset
+              <select id="bayesPreset" onchange="applyBayesPreset(this.value)" style="height:32px;border-radius:8px;border:1px solid var(--border);background:#fff;padding:0 8px;font-size:0.8rem">
+                <option value="">— manuel —</option>
+                <option value="zayif_prior">zayıf prior</option>
+                <option value="dengeli">dengeli</option>
+                <option value="guclu_prior">güçlü prior</option>
+                <option value="evidence_agir">evidence ağır</option>
+                <option value="sadece_prior">sadece prior</option>
+              </select>
+            </label>
           </div>
+          <div class="adv-hint" style="margin-top:8px">α: 0.5–1 zayıf · 1–3 dengeli · 5+ güçlü. n: 0=sadece prior · 5–15 orta · 30+ yüksek güven. KL: &lt;0.02 ihmal · 0.1–0.3 orta · &gt;0.7 güçlü kayma.</div>
         </div>
         <div class="options-row">
           <div class="opt-group">
@@ -302,7 +313,13 @@
         <div class="kumulatif" id="kumulatifText">Tüm dağılım gösteriliyor.</div>
         {% endif %}
         {% if result.advanced %}
-        <div class="card-title" style="margin-top:8px">Gelişmiş Olasılık {% if result.advanced.source == 'bayes_posterior' %}(Bayes posterior){% endif %}</div>
+        <div class="card-title" style="margin-top:8px">Gelişmiş Olasılık
+          {% if result.advanced.source == 'bayes_posterior' %}
+          <span style="font-size:0.72rem;font-weight:600;color:#1a7a3a;background:var(--success-bg);padding:2px 8px;border-radius:6px;margin-left:6px">kaynak: Bayes posterior</span>
+          {% else %}
+          <span style="font-size:0.72rem;font-weight:500;color:var(--text-sec);margin-left:6px">kaynak: kullanıcı probs</span>
+          {% endif %}
+        </div>
         <div class="mc-grid">
           <div class="mc-box"><div class="mc-label">Küme içi</div><div class="mc-val">%{{ result.advanced.exact.p_kume_ici }}</div></div>
           <div class="mc-box"><div class="mc-label">P(15)</div><div class="mc-val">%{{ result.advanced.exact.p_15 }}</div></div>
@@ -320,12 +337,17 @@
         {% endif %}
         {% if result.bayes %}
         <div class="card-title" style="margin-top:10px">Bayes Posterior (Dirichlet)</div>
-        <div style="font-size:0.78rem;color:var(--text-sec);margin-bottom:8px">
+        <div style="font-size:0.78rem;color:var(--text-sec);margin-bottom:6px">
           Prior α={{ result.bayes.prior_strength }} · Evidence n={{ result.bayes.evidence_strength }}
           · Ort. KL={{ result.bayes.summary.mean_kl_prior_post }}
+          {% if result.bayes.summary.mean_kl_label %} · <strong>{{ result.bayes.summary.mean_kl_label }}</strong>{% endif %}
+        </div>
+        <div style="font-size:0.72rem;color:var(--text-sec);margin-bottom:8px">
+          14-garanti kombinatoryaldir; Bayes yalnızca olasılık ağırlıklarını günceller.
+          {% if result.advanced and result.advanced.source == 'bayes_posterior' %} · Kaynak: <strong>posterior</strong>{% endif %}
         </div>
         <table class="bayes-table">
-          <thead><tr><th>Maç</th><th>Prior 1/0/2</th><th>Evidence</th><th>Posterior</th><th>KL</th></tr></thead>
+          <thead><tr><th>Maç</th><th>Prior 1/0/2</th><th>Evidence</th><th>Posterior</th><th>KL</th><th>Yorum</th></tr></thead>
           <tbody>
             {% for m in result.bayes.matches %}
             <tr>
@@ -333,7 +355,8 @@
               <td>{{ m.prior['1'] }}/{{ m.prior['0'] }}/{{ m.prior['2'] }}</td>
               <td>{{ m.evidence['1'] }}/{{ m.evidence['0'] }}/{{ m.evidence['2'] }}</td>
               <td><strong>{{ m.posterior['1'] }}</strong>/{{ m.posterior['0'] }}/{{ m.posterior['2'] }}</td>
-              <td>{{ m.kl }}</td>
+              <td>{{ '%.4f'|format(m.kl) }}</td>
+              <td>{{ m.kl_label or '—' }}</td>
             </tr>
             {% endfor %}
           </tbody>
@@ -438,9 +461,9 @@
           {% endfor %}
         </div>
         {% if result.guaranteed %}
-        <div class="alert alert-ok" style="margin-top:12px">En kötü 1 hata — <strong>14-Garanti doğrulandı</strong></div>
+        <div class="alert alert-ok" style="margin-top:12px">En kötü mesafe ≤ 1 — <strong>14-Garanti doğrulandı</strong>. Seçim kümesi içinde doğru sonuç varsa en az bir kolon en fazla 1 maç hatalı kalır.</div>
         {% else %}
-        <div class="alert alert-warn" style="margin-top:12px">En kötü {{ result.worst }} hata — garanti yok.</div>
+        <div class="alert alert-warn" style="margin-top:12px">En kötü mesafe {{ result.worst }} — <strong>14-garanti yok</strong>. Fix-16 veya bütçeyi yükseltin.</div>
         {% endif %}
       </div>
     {% else %}
@@ -498,7 +521,21 @@
   function hideClientError(){ document.getElementById('clientError').classList.remove('show'); }
   document.getElementById('mainForm').addEventListener('submit', function(e){
     const mode=document.getElementById('mode').value, budget=document.getElementById('budget').value;
-    if((mode==='butce'||mode==='maxcov')&&(!budget||budget<1)){ e.preventDefault(); showClientError('Bütçe giriniz.'); return; }
+    if((mode==='butce'||mode==='maxcov')&&(!budget||Number(budget)<1)){
+      e.preventDefault(); showClientError('Bütçe modu için pozitif kolon bütçesi giriniz.'); return;
+    }
+    if(mode==='fix16'){
+      let cifte=0;
+      for(let i=1;i<={{ match_count }};i++){
+        const n=document.querySelectorAll('input[name="match_'+i+'"]:checked').length||3;
+        if(n===2) cifte++;
+      }
+      if(cifte<7){
+        e.preventDefault();
+        showClientError('Fix-16 en az 7 çifte maç ister (şu an '+cifte+'). Bir maçı daha çifte yapın veya Mod=auto seçin.');
+        return;
+      }
+    }
     hideClientError(); document.getElementById('submitBtn').disabled=true; document.getElementById('loading').classList.add('active');
   });
   function copyTable(){
@@ -521,20 +558,81 @@
     const btn=document.getElementById('imgBtn'); btn.textContent='İndirildi'; btn.classList.add('done'); setTimeout(()=>{ btn.textContent='Görsel İndir'; btn.classList.remove('done'); },1800);
   }
   const STORAGE_KEY='spor_toto_saved_coupons';
-  function getSaved(){ try{ return JSON.parse(localStorage.getItem(STORAGE_KEY)||'{}'); }catch{ return {}; } }
+  const BAYES_PRESETS={
+    zayif_prior:{prior:0.5,evidence:10},
+    dengeli:{prior:1,evidence:10},
+    guclu_prior:{prior:5,evidence:10},
+    evidence_agir:{prior:1,evidence:30},
+    sadece_prior:{prior:1,evidence:0}
+  };
+  function applyBayesPreset(key){
+    if(!key||!BAYES_PRESETS[key]) return;
+    const p=BAYES_PRESETS[key];
+    const a=document.getElementById('priorStrength'), n=document.getElementById('evidenceStrength'), cb=document.getElementById('useBayes');
+    if(a) a.value=p.prior; if(n) n.value=p.evidence; if(cb) cb.checked=true;
+  }
+  function getSaved(){
+    try{
+      const raw=localStorage.getItem(STORAGE_KEY);
+      if(!raw) return {};
+      const data=JSON.parse(raw);
+      return (data && typeof data==='object' && !Array.isArray(data)) ? data : {};
+    }catch(e){ return {}; }
+  }
+  function persistSaved(data){
+    try{ localStorage.setItem(STORAGE_KEY, JSON.stringify(data)); return true; }
+    catch(e){ alert('Kayıt yapılamadı (tarayıcı depolama dolu veya engelli).'); return false; }
+  }
   function saveCoupon(){
-    const name=document.getElementById('couponName').value.trim(); if(!name){ alert('Kupon adı giriniz.'); return; }
-    const selections=[]; for(let i=1;i<={{ match_count }};i++){ const checked=[...document.querySelectorAll('input[name="match_'+i+'"]:checked')].map(cb=>cb.value); selections.push(checked.length?checked:['1','0','2']); }
-    const data=getSaved(); data[name]={selections, mode:document.getElementById('mode').value, ts:Date.now()}; localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
-    document.getElementById('couponName').value=''; renderSavedList();
+    const name=document.getElementById('couponName').value.trim();
+    if(!name){ alert('Kupon adı giriniz.'); return; }
+    const selections=[];
+    for(let i=1;i<={{ match_count }};i++){
+      const checked=[...document.querySelectorAll('input[name="match_'+i+'"]:checked')].map(cb=>cb.value);
+      selections.push(checked.length?checked:['1','0','2']);
+    }
+    const data=getSaved();
+    if(data[name] && !confirm('«'+name+'» zaten var. Üzerine yazılsın mı?')) return;
+    data[name]={selections, mode:document.getElementById('mode').value, ts:Date.now()};
+    if(!persistSaved(data)) return;
+    document.getElementById('couponName').value='';
+    renderSavedList();
+  }
+  function loadCoupon(name){
+    const c=getSaved()[name]; if(!c||!Array.isArray(c.selections)) return;
+    c.selections.forEach((syms,idx)=>{
+      document.querySelectorAll('input[name="match_'+(idx+1)+'"]').forEach(cb=>{ cb.checked=Array.isArray(syms)&&syms.includes(cb.value); });
+    });
+    if(c.mode){ document.getElementById('mode').value=c.mode; toggleBudget(c.mode); }
+    updateLiveStats(); hideClientError();
+  }
+  function deleteCoupon(name){
+    if(!confirm('«'+name+'» silinsin mi?')) return;
+    const data=getSaved(); delete data[name]; persistSaved(data); renderSavedList();
   }
-  function loadCoupon(name){ const c=getSaved()[name]; if(!c) return; c.selections.forEach((syms,idx)=>{ document.querySelectorAll('input[name="match_'+(idx+1)+'"]').forEach(cb=>{ cb.checked=syms.includes(cb.value); }); }); if(c.mode){ document.getElementById('mode').value=c.mode; toggleBudget(c.mode); } updateLiveStats(); }
-  function deleteCoupon(name){ const data=getSaved(); delete data[name]; localStorage.setItem(STORAGE_KEY, JSON.stringify(data)); renderSavedList(); }
   function renderSavedList(){
-    const data=getSaved(), el=document.getElementById('savedList'), names=Object.keys(data);
+    const data=getSaved(), el=document.getElementById('savedList'), names=Object.keys(data).sort();
     if(!names.length){ el.innerHTML='<div style="color:#6e6e73;font-size:0.78rem">Kayıtlı kupon yok.</div>'; return; }
-    el.innerHTML=names.map(n=>'<div class="saved-item"><span>'+n+'</span><span><button type="button" onclick="loadCoupon(\''+n+'\')">Yükle</button> <button type="button" onclick="deleteCoupon(\''+n+'\')">Sil</button></span></div>').join('');
+    el.innerHTML=names.map(n=>{
+      const safe=String(n).replace(/\\/g,'\\\\').replace(/'/g,"\\'");
+      return '<div class="saved-item"><span>'+n+'</span><span><button type="button" onclick="loadCoupon(\''+safe+'\')">Yükle</button> <button type="button" onclick="deleteCoupon(\''+safe+'\')">Sil</button></span></div>';
+    }).join('');
   }
+  const HEALTH_TIPS={
+    encoder:'Encoder hatası: seçim/girdi katmanını kontrol edin.',
+    fix16_garanti:'Fix-16 garantisi kırıldı — core regressiyonu olabilir.',
+    fix16_yetersiz_cifte:'6 çifte reddi çalışmıyor olabilir.',
+    distance_layers:'Mesafe katmanları tutarsız.',
+    blok_motor:'Blok motoru beklenen bedeli aştı.',
+    heuristic:'Heuristik beklenenden kötü sonuç.',
+    olasilik_exact:'Exact olasılık raporu bozuldu.',
+    monte_carlo:'Monte Carlo sapması yüksek veya hata.',
+    bayes_dirichlet:'Bayes posterior normalize/NaN sorunu.',
+    markov_chain:'Markov özeti şekil hatası.',
+    error_freq:'Hata frekansı hesabı başarısız.',
+    pipeline_result_shape:'Pipeline çıktı şekli beklenen değil.',
+    scipy_flag:'scipy kurulu mu? (pip install scipy)'
+  };
   async function runHealth(){
     const btn=document.getElementById('healthBtn'), sum=document.getElementById('healthSummary'), list=document.getElementById('healthList');
     btn.disabled=true; btn.textContent='Çalışıyor…'; sum.style.display='none'; list.innerHTML='';
@@ -542,15 +640,22 @@
       const res=await fetch('/health'); const data=await res.json();
       sum.style.display='block';
       if(data.ok){ sum.className='alert alert-ok'; sum.innerHTML='<strong>HEALTHY</strong> — '+data.passed+'/'+data.total+' geçti · v'+data.version; }
-      else { sum.className='alert alert-error'; sum.innerHTML='<strong>UNHEALTHY</strong> — '+data.failed+' hata / '+data.total+' test'; }
+      else { sum.className='alert alert-error'; sum.innerHTML='<strong>UNHEALTHY</strong> — '+data.failed+' hata / '+data.total+' test · fail satırlarına bakın'; }
       list.innerHTML=(data.checks||[]).map(function(c){
-        return '<div class="health-row '+(c.ok?'ok':'fail')+'">'+'<span class="health-badge '+(c.ok?'ok':'fail')+'">'+(c.ok?'OK':'FAIL')+'</span>'+'<span class="health-name">'+c.name+'</span>'+'<span class="health-ms">'+c.duration_ms+' ms</span>'+'<div class="health-detail">'+(c.detail||'')+'</div></div>';
+        let tip='';
+        if(!c.ok){ tip=HEALTH_TIPS[c.name]||'pytest / python -m spor_toto.health ile tekrarlayın.'; }
+        return '<div class="health-row '+(c.ok?'ok':'fail')+'">'+ 
+          '<span class="health-badge '+(c.ok?'ok':'fail')+'">'+(c.ok?'OK':'FAIL')+'</span>'+
+          '<span class="health-name">'+c.name+'</span>'+
+          '<span class="health-ms">'+c.duration_ms+' ms</span>'+
+          '<div class="health-detail">'+(c.detail||'')+(tip?'<div style="margin-top:4px;color:#ff3b30">→ '+tip+'</div>':'')+'</div></div>';
       }).join('');
-    }catch(e){ sum.style.display='block'; sum.className='alert alert-error'; sum.textContent='Health erişilemedi: '+e.message; }
+    }catch(e){ sum.style.display='block'; sum.className='alert alert-error'; sum.textContent='Health erişilemedi: '+e.message+' — sunucu ayakta mı?'; }
     finally{ btn.disabled=false; btn.textContent='Testleri Çalıştır'; }
   }
   updateLiveStats(); renderSavedList();
   {% if result and result.advanced %}document.getElementById('advPanel').classList.add('open');{% endif %}
+  {% if result and result.bayes %}document.getElementById('advPanel').classList.add('open');{% endif %}
 </script>
 </body>
 </html>
